AUTORIZACIÓN HUMANA — ALTERNATIVA C RATIFICADA
==============================================

Añade este bloque AL PRINCIPIO de 01_TAREA_ACTUAL.txt y vuelve a ejecutar
02_INICIAR_AUTOREGENERACION.cmd.

DECISIÓN RATIFICADA:

Se autoriza la alternativa C: separar la acción del pipeline de la decisión
de negocio.

Implementación requerida:

1. Introducir un campo independiente:

   pipeline_action

   Valores permitidos en esta fase:
   - PROTOTIPAR
   - DIVERGIR

2. Conservar:

   recommended_status

   limitado estrictamente a VALID_DECISIONS:
   - ADOPTAR
   - AMPLIAR PRUEBA
   - ABANDONAR
   - ARCHIVAR PARA RECOMBINAR

3. No mapear PROTOTIPAR directamente a ADOPTAR.

4. Para la lógica actual basada únicamente en número de familias:

   pipeline_action =
       PROTOTIPAR si len(families) >= 4
       DIVERGIR si len(families) < 4

   recommended_status = "AMPLIAR PRUEBA"

   Esta decisión es deliberadamente conservadora: el número de familias por sí
   solo no autoriza ADOPTAR.

5. ADOPTAR solo podrá emitirse cuando exista una regla explícita y probada
   basada en evidencia suficiente, no simplemente por len(families) >= 4.

6. Migrar el contrato, tests y golden de manera aditiva:
   - no eliminar recommended_status;
   - añadir pipeline_action;
   - actualizar schema/version si corresponde;
   - documentar la migración;
   - verificar compatibilidad hacia atrás.

7. Verificar:
   - recommended_status siempre pertenece a VALID_DECISIONS;
   - pipeline_action pertenece a {PROTOTIPAR, DIVERGIR};
   - el test existente de AMPLIAR PRUEBA continúa pasando;
   - el golden cambia únicamente por la adición/corrección documentada;
   - la fórmula value_score y la frontera generador/medidor permanecen intactas.

8. Una vez verde, marcar FASE 0 VERIFIED y continuar con FASE 1.

No vuelvas a pedir decisión A/B/C: la alternativa C queda ratificada.