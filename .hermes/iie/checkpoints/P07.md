# P07 CHECKPOINT — SIGNAL ENGINE

- **PHASE**: P07 SIGNAL ENGINE
- **STATUS**: PARTIAL
- **START_COMMIT**: 68f2a92
- **END_COMMIT**: 62f0c16 (P07-T04 code; metadata checkpoint follows)
- **COMPLETED_TASKS**:
  - P07-T01 deterministic time-series observations
  - P07-T02 discrete velocity + acceleration
  - P07-T03 burst detection
  - P07-T04 change-point framework
- **FAILED_TASKS**: ninguna
- **DEFERRED_TASKS**:
  - BF-P00-T06: validate tracked BLACKFORGE goldens in an isolated copy; live artifacts remain untouched
- **FILES_CREATED**:
  - `src/criba/intelligence/signals/dynamics.py`
  - `src/criba/intelligence/signals/bursts.py`
  - `src/criba/intelligence/signals/changepoints.py`
  - `tests/intelligence/test_signal_observations.py`
  - `tests/intelligence/test_signal_bursts.py`
  - `tests/intelligence/test_signal_changepoints.py`
  - `.hermes/iie/tasks/P07-T01.json`
  - `.hermes/iie/tasks/P07-T02.json`
  - `.hermes/iie/tasks/P07-T03.json`
  - `.hermes/iie/tasks/P07-T04.json`
- **FILES_MODIFIED**:
  - `src/criba/intelligence/signals/__init__.py`
  - `src/criba/intelligence/signals/dynamics.py`
  - `tests/intelligence/test_signal_observations.py`
  - `src/criba/intelligence/storage/store.py`
  - `.hermes/iie/STATE.json`
  - `.hermes/iie/CONTINUITY.md`
- **CONTRACT_CHANGES**:
  - Added `ObservationSeries`, a deterministic topic/period view over `TopicObservation` values.
  - Added isolated `IntelligenceStore.save_observation()` and `list_observations()` persistence methods.
  - Added `TopicDynamics.velocity()` and `TopicDynamics.acceleration()` as period-aligned discrete metrics.
  - Added `BurstDetector` and `BurstEvent` for deterministic upward velocity threshold detection.
  - Added `ChangePointDetector` and `ChangePointEvent` for deterministic two-window level shifts.
  - Observation metadata is JSON-safe on disk and decoded on read; listing supports topic filtering and bounded results.
- **DATABASE_CHANGES**: no legacy database changes; observations use the existing `intel_topic_observations` table in the isolated IntelligenceStore database; schema remains version 1.
- **API_CHANGES**: additive internal signal/storage API only; no REST/MCP changes.
- **FEATURE_FLAGS**: all IIE flags remain `false`.
- **TESTS**:
  - `python -m pytest tests/intelligence/test_signal_changepoints.py tests/intelligence/test_signal_bursts.py tests/intelligence/test_signal_observations.py -q -p no:cacheprovider` → 7 passed
  - focused signal/storage/contract regression → 25 passed
  - `python -m pytest -q -p no:cacheprovider` → 781 passed, 1 pre-existing dependency deprecation warning
  - strict mypy over 5 touched source files → success
  - compileall over signals/storage → success
  - `git diff --check` → success
- **MODEL USAGE**: P07-T01 through P07-T04 executed by GPT-5.6 Terra in the high reasoning slot specified by the blueprint.
- **CREDIT STATUS**: GREEN; token telemetry unavailable.
- **KNOWN RISKS**:
  - Period ordering is lexical and expects canonical comparable period values such as `YYYY-MM`.
  - Persistence is append-only; aggregation remains deferred to subsequent P07 tasks.
  - Burst threshold is explicit and fixed; adaptive baselines are deferred.
  - Change-point windows may overlap around prolonged transitions; consolidation is deferred.
  - Velocity and acceleration assume equally spaced periods; irregular calendar gaps are not normalized.
  - Empty topic/period values and negative counters remain permitted by the existing contract.
  - Live BLACKFORGE goldens were not modified.
- **TECHNICAL DEBT**: `uuid4` in `engine.py:1015` remains pre-existing and outside IIE scope.
- **ROLLBACK POINT**: `68f2a92` is the P07 start checkpoint; use new `git revert b24efbd`, `git revert 594aa56`, `git revert 1a7063a`, and `git revert 62f0c16` commits as needed; do not rewrite history or delete databases.
- **NEXT PHASE**: P07 SIGNAL ENGINE
- **NEXT EXACT TASK**: P07-T05 — anomaly detection.
