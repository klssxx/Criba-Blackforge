# P07 CHECKPOINT — SIGNAL ENGINE

- **PHASE**: P07 SIGNAL ENGINE
- **STATUS**: PASSED
- **START_COMMIT**: 68f2a92
- **END_COMMIT**: (P07-T10 code; metadata checkpoint follows)
- **COMPLETED_TASKS**:
  - P07-T01 deterministic time-series observations
  - P07-T02 discrete velocity + acceleration
  - P07-T03 burst detection
  - P07-T04 change-point framework
  - P07-T05 anomaly detection
  - P07-T06 weak-signal aggregation
  - P07-T07 lead/lag framework
  - P07-T08 S-curve approximation
  - P07-T09 synthetic time-series tests
  - P07-T10 signal semantics audit
- **FAILED_TASKS**: ninguna
- **DEFERRED_TASKS**:
  - BF-P00-T06: validate tracked BLACKFORGE goldens in an isolated copy; live artifacts remain untouched
- **FILES_CREATED**:
  - `src/criba/intelligence/signals/dynamics.py`
  - `src/criba/intelligence/signals/bursts.py`
  - `src/criba/intelligence/signals/changepoints.py`
  - `src/criba/intelligence/signals/anomaly.py`
  - `src/criba/intelligence/signals/weak_signals.py`
  - `src/criba/intelligence/signals/lead_lag.py`
  - `src/criba/intelligence/signals/scurve.py`
  - `tests/intelligence/test_signal_observations.py`
  - `tests/intelligence/test_signal_bursts.py`
  - `tests/intelligence/test_signal_changepoints.py`
  - `tests/intelligence/test_signal_anomaly.py`
  - `tests/intelligence/test_signal_weak_signals.py`
  - `tests/intelligence/test_signal_lead_lag.py`
  - `tests/intelligence/test_signal_scurve.py`
  - `tests/intelligence/signal_fixtures.py`
  - `tests/intelligence/test_signal_synthetic.py`
  - `tests/intelligence/test_signal_semantics.py`
  - `.hermes/iie/tasks/P07-T01.json` through `.hermes/iie/tasks/P07-T10.json`
- **FILES_MODIFIED**:
  - `src/criba/intelligence/signals/__init__.py`
  - `src/criba/intelligence/storage/store.py`
  - `.hermes/iie/STATE.json`
  - `.hermes/iie/CONTINUITY.md`
- **CONTRACT_CHANGES**:
  - Added `ObservationSeries`, a deterministic topic/period view over `TopicObservation` values.
  - Added isolated `IntelligenceStore.save_observation()` and `list_observations()` persistence methods.
  - Added `TopicDynamics.velocity()` and `TopicDynamics.acceleration()` as period-aligned discrete metrics.
  - Added `BurstDetector`/`BurstEvent`, `ChangePointDetector`/`ChangePointEvent`, `AnomalyDetector`/`AnomalyEvent`.
  - Added `WeakSignalAggregator` with semantic grouping, signal-ID deduplication and provenance union.
  - Added `LeadLagAnalyzer`/`LeadLagResult` for bounded cross-topic Pearson lag analysis.
  - Added `SCurveApproximator`/`SCurveFit` explainable logistic approximation with strict in-asymptote clamping.
- **DATABASE_CHANGES**: no legacy database changes; observations use the existing `intel_topic_observations` table in the isolated IntelligenceStore database; schema remains version 1.
- **API_CHANGES**: additive internal signal/storage API only; no REST/MCP changes.
- **FEATURE_FLAGS**: all IIE flags remain `false`.
- **TESTS**:
  - focused signal/storage/contract regression → 52 passed
  - `python -m pytest -q -p no:cacheprovider` → 808 passed, 1 pre-existing dependency deprecation warning
  - strict mypy over signals + storage → success
  - compileall over signals → success
  - AST import audit: no numeric/graph external library imports in the signal sector
  - `git diff --check` → success
- **MODEL USAGE**: P07-T01 through P07-T08 executed by GPT-5.6 Terra in the blueprint slots; P07-T09 by GPT-5.6 Luna medium; P07-T10 audit by GPT-5.6 Sol high.
- **CREDIT STATUS**: GREEN; token telemetry unavailable.
- **KNOWN RISKS**:
  - Period ordering is lexical and expects canonical comparable period values such as `YYYY-MM`.
  - Burst threshold is explicit and fixed; adaptive baselines are deferred.
  - Change-point windows may overlap around prolonged transitions; consolidation is deferred.
  - The zero-MAD anomaly fallback uses one count unit; domain calibration is deferred.
  - Independent-support aggregation can overstate confidence for correlated signals; correlation-aware weighting is deferred.
  - Correlation does not establish causality; shared trends can produce spurious lead/lag relationships.
  - Velocity and acceleration assume equally spaced periods; irregular calendar gaps are not normalized.
  - S-curve capacity is range-derived, not domain-calibrated; substitution curves are deferred.
  - Live BLACKFORGE goldens were not modified.
- **TECHNICAL DEBT**: `uuid4` in `engine.py:1015` remains pre-existing and outside IIE scope.
- **ROLLBACK POINT**: `68f2a92` is the P07 start checkpoint; revert P07 code commits (`b24efbd`, `594aa56`, `1a7063a`, `62f0c16`, `ca89e8c`, `7400168`, `e638dbc`, `a4da1e6`, `4db46bc` and the T10 audit commit) with new `git revert` commits as needed; do not rewrite history or delete databases.
- **NEXT PHASE**: P08 GAP ENGINE
- **NEXT EXACT TASK**: P08-T01 — research-gap extraction.
