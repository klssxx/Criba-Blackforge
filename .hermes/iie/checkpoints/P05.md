# P05 CHECKPOINT — PROVENANCE + ENTITIES

- **PHASE**: P05 PROVENANCE + ENTITIES
- **STATUS**: PASSED
- **START_COMMIT**: 709f16b
- **END_COMMIT**: 89b0657 (P05-T03 code commit; metadata checkpoint follows)
- **COMPLETED_TASKS**:
  - P05-T01 epistemic semantics: FACT grounding and deterministic downgrade
  - P05-T02 provenance implementation and content hashing
  - P05-T03 isolated claim store: save/get/list/upsert with JSON decoding and column whitelist
  - P05-T04 deterministic entity extraction
  - P05-T05 type-aware entity resolution
  - P05-T06 aliases and normalization
  - P05-T07 provenance invariant tests
- **FAILED_TASKS**: ninguna
- **DEFERRED_TASKS**:
  - BF-P00-T06: validate tracked BLACKFORGE goldens in an isolated copy; live artifacts remain untouched
- **FILES_CREATED**:
  - `tests/intelligence/test_claim_store.py`
  - `.hermes/iie/tasks/P05-T03.json`
  - `.hermes/iie/checkpoints/P05.md`
- **FILES_MODIFIED**:
  - `src/criba/intelligence/storage/store.py`
  - `.hermes/iie/STATE.json`
  - `.hermes/iie/CONTINUITY.md`
- **CONTRACT_CHANGES**:
  - `IntelligenceStore.save_claim()` now accepts canonical claim payloads safely.
  - Added `get_claim()` and `list_claims(run_id, epistemic_state, limit)`.
  - Public dataclass contracts unchanged.
- **DATABASE_CHANGES**: no legacy database changes; isolated `intel_claims` table only; schema remains version 1.
- **API_CHANGES**: additive internal IntelligenceStore methods; no REST/MCP changes.
- **FEATURE_FLAGS**: all IIE flags remain `false`.
- **TESTS**:
  - `python -m pytest tests/intelligence -q -p no:cacheprovider` → 65 passed
  - `python -m pytest -q -p no:cacheprovider` → 746 passed, 1 warning
  - strict mypy over storage/claims/provenance/entities → success
  - `git diff --check` → success
- **MODEL USAGE**: P05-T03 executed by GPT-5.6 Luna; previous P05 task usage remains recorded in their journals.
- **CREDIT STATUS**: GREEN; token telemetry unavailable.
- **KNOWN RISKS**:
  - Claim list ordering relies on SQLite rowid because `intel_claims` has no created_at column.
  - Live BLACKFORGE goldens were not modified.
- **TECHNICAL DEBT**: `uuid4` in `engine.py:1015` remains pre-existing and outside IIE scope.
- **ROLLBACK POINT**: P04 commit `709f16b`; use a new `git revert 89b0657` commit for the P05-T03 code change; do not rewrite history.
- **NEXT PHASE**: P06 GRAPH
- **NEXT EXACT TASK**: P06-T01 — implement the SQLite-backed graph store without changing legacy storage.
