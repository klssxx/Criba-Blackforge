# P04 CHECKPOINT — RETRIEVAL

- **PHASE**: P04 RETRIEVAL
- **STATUS**: VERIFIED (deterministic local behavior)
- **START_COMMIT**: `3cea520`
- **END_COMMIT**: recorded by the recovery-audit commit following this checkpoint
- **COMPLETED_TASKS**:
  - **P04-T01** lexical FTS/BM25: `LexicalIndex` BM25-lite over `intel_documents`, deterministic and offline.
  - **P04-T02** hybrid score contract: reciprocal-rank fusion plus source-diversity reranking.
  - **P04-T03** query expansion: deterministic synonym and ontology expansion.
  - **P04-T04** query decomposition: conjunction-based decomposition in `QueryExpander`.
  - **P04-T05** query mutation: deterministic T122-tagged variants for the prior-art rejection loop.
  - **P04-T06** recursive search: retrieve → salient-term expansion → re-query with depth/query budgets.
  - **P04-T07** citation chasing: backward and forward traversal only over explicit `metadata.references` edges.
  - **P04-T08** multilingual query generation interface: finite English-to-Spanish curated mappings as tagged query variants.
  - **P04-T09** dedup/rerank/fusion: Jaccard near-duplicate collapse, RRF and diversity cap; cardinality validation and stable document-ID tie breaks added in recovery audit.
  - **P04-T10** retrieval quality review: corrected initial-query execution at `max_queries=1`; added regression coverage for that case and deterministic fusion behavior.
- **EVIDENCE**:
  - `python -m pytest tests/intelligence/test_retrieval.py tests/intelligence/test_boundaries.py -q -p no:cacheprovider` → **17 passed**.
  - `python -m pytest tests/intelligence -q -p no:cacheprovider` → **182 passed**.
  - `python -m compileall -q src/criba/intelligence/retrieval` → success.
  - `git diff --check` → clean.
- **HISTORICAL NOTE**: the older P04 ledger used capability-oriented titles for T04–T06. Those records are now mapped back to the master-blueprint task IDs without rewriting historical implementation commits. The prior checkpoint's 52/733 results remain historical claims and were not reused as current evidence.
- **KNOWN LIMITS**:
  - BM25-lite is token-based and non-positional; no live corpus quality benchmark was run.
  - `new_doc_bonus_threshold` is accepted by `RecursiveSearcher` but is not an active stopping policy.
  - Citation completeness depends on the sources that populate `metadata.references` in later phases.
- **ROLLBACK POINT**: `32ff58b` (P03 checkpoint); P04 recovery changes are isolated in their own commit.
- **NEXT PHASE**: P05 PROVENANCE.
- **NEXT EXACT TASK**: P05-T01 epistemic semantics: grounded-claim ratio (FACT requires evidence, §102).
